{
  "name": "heartlines-relation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "browser:smoke": "node tools/browser-smoke.mjs",
    "check": "node --check server.mjs && node --check src/model.mjs && node --check src/graph.mjs && node --check src/app.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=24"
  }
}
